# Copy to secrets.yaml, then replace every placeholder. Never commit secrets.yaml.
wifi_ssid: "YOUR_2_4_GHZ_WIFI_SSID"
wifi_password: "YOUR_WIFI_PASSWORD"
# Generate locally: openssl rand -base64 32
burner_camera_api_key: "REPLACE_WITH_A_GENERATED_32_BYTE_BASE64_KEY"
burner_camera_ota_password: "CHOOSE_A_UNIQUE_OTA_PASSWORD"
